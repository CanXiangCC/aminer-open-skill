"""Adapters: isolation layer between production and the frozen pack / benchmark.

``rule_pack.py``  — sys.path + lazy imports of pack rule/ML strategies.
``wf8_llm.py``    — bridges the frozen 板块 5 wf8 pipeline as the llm_group.
"""
